name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      name: 
        description: 'Just a name'
        required: true
        default: 'World'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Download Private Repository
        run: |
          cd ~/
          git clone https://github.com/tjovcheska/ToDo.git
          cd ToDo
          ls -la
